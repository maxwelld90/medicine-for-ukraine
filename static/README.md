# This is the static directory.

We can run the `collectstatic` command on the Django instance to pull static content to this directory.
This directory's contents are served at the `static` subdomain.